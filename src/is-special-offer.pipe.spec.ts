import { IsSpecialOfferPipe } from './is-special-offer.pipe';

describe('IsSpecialOfferPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSpecialOfferPipe();
    expect(pipe).toBeTruthy();
  });
});
